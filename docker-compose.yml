services:
  smartmirror:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DISPLAY=host.docker.internal:0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    platform: linux/arm/v7
    restart: unless-stopped
